{{- if .Values.services.booking.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "appointments-app.service.name" (dict "context" . "name" "booking") }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "appointments-app.service.labels" (dict "context" . "name" "booking") | nindent 4 }}
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "{{ .Values.services.booking.port }}"
    prometheus.io/path: "/metrics"
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.services.booking.port }}
    targetPort: {{ .Values.services.booking.port }}
    protocol: TCP
    name: http
  selector:
    {{- include "appointments-app.service.selectorLabels" (dict "context" . "name" "booking") | nindent 4 }}
{{- end }}