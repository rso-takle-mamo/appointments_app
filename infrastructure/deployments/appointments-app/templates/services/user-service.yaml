{{- if .Values.services.user.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "appointments-app.service.name" (dict "context" . "name" "user") }}
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "appointments-app.service.labels" (dict "context" . "name" "user") | nindent 4 }}
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "{{ .Values.services.user.port }}"
    prometheus.io/path: "/metrics"
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.services.user.port }}
    targetPort: {{ .Values.services.user.port }}
    protocol: TCP
    name: http
  selector:
    {{- include "appointments-app.service.selectorLabels" (dict "context" . "name" "user") | nindent 4 }}
{{- end }}