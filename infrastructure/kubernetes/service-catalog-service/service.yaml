apiVersion: v1
kind: Service
metadata:
  name: service-catalog-service
spec:
  type: ClusterIP
  selector:
    app: service-catalog-service
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
    name: http